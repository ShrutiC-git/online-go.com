error_log /var/log/nginx/error.log info;

events {
    worker_connections 1024; #default
}


http {
    server {

        listen 8081;

        location / {
            proxy_pass http://localhost:8080/;
        }
    }
}